PORT=5000
NODE_ENV=development

MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority
JWT_SECRET=change-me

# Fix for SRV DNS issues on some networks/VPNs (comma-separated)
# DNS_SERVERS=1.1.1.1,8.8.8.8

# Required for correct local/static image URLs when using local uploads
PUBLIC_BASE_URL=http://localhost:5000

# Optional (recommended for deployment): enable Cloudinary storage
CLOUDINARY_CLOUD_NAME=dctsshl6v
CLOUDINARY_API_KEY=847795561939397
CLOUDINARY_API_SECRET=mepteytv_t5m3QI2Az6huDxxiKA

# Optional: restrict allowed origins (comma separated)
# CORS_ORIGIN=http://localhost:19006,http://localhost:8081

